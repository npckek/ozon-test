## Expense Tracker — Тестовое задание

### Инструмент для учёта личных трат, выполненный в рамках тестового задания на позицию Frontend Developer.
🛠 Технологический стек
- Framework: Next.js (App Router) — выбран как современный мета-фреймворк с отличной производительностью и встроенным роутингом.
- Language: TypeScript — для обеспечения типобезопасности и минимизации ошибок при работе с данными.
- Styling: Tailwind CSS — для быстрой и адаптивной вёрстки с использованием утилитарных классов.
- Charts: Recharts — легкая библиотека для визуализации статистики расходов.
- State: React Hooks (useState, useMemo, useEffect) + localStorage для персистентности данных.

🚀 Как запустить проект

Клонируйте репозиторий:
```Bash
git clone https://github.com/npckek/ozon-test
cd ./ozon-test
```

Установите зависимости:
```Bash
npm install
```

Запустите сервер для разработки:
```Bash
npm run dev
```

Откройте приложение: Перейдите по адресу http://localhost:3000

### ✨ Реализованные усложнения

Я постарался реализовать максимум дополнительных возможностей:

- TypeScript: Полная типизация интерфейсов Expense, категорий и пропсов компонентов.
- Мета-фреймворк: Использован Next.js.
- Фильтрация по категории: Добавлен Dropdown для фильтрации истории операций.
- Фильтрация по датам: Реализован выбор диапазона "от" и "до".
- Редактирование траты: Возможность изменить любую запись из списка с предзаполнением формы.
- Диаграмма расходов: Визуализация распределения трат по категориям с помощью Pie Chart.

### 📝 Что реализовано (согласно ТЗ)
- Форма: Валидация полей (сумма > 0, описание от 3 символов, дата не в будущем), блокировка кнопки при ошибках, автофокус после добавления.
- Список: Сортировка (новые сверху), форматирование дат (DD.MM.YYYY) и сумм (разделитель тысяч + ₽).
- Итоги: Динамический пересчёт общей суммы и группировка по категориям (скрытие пустых).
- Хранение: Автоматическая синхронизация с localStorage (ключ expenses).